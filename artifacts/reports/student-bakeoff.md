# Student bake-off

Status: `NOT RUN`

No candidate results are populated. The ground-truth NodeLM plan is unavailable, so the exact
candidate list, model revisions, licenses, architectures, and context limits remain
`UNVERIFIED`.

When measured, each candidate row must include precision, backend, GPU memory, prompt and decode
speed, tool-call validity, task resolution, regression-test result, token counts, and wall-clock
duration with command/config evidence.
