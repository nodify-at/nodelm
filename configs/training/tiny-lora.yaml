schema_version: nodelm.training-config/v1
status: UNVERIFIED
purpose: tiny LoRA/PEFT smoke test
model:
  repository_id: null
  revision: null
  license: null
runtime: null
precision: null
seed: 42
max_steps: 1
batch_size: 1
gradient_accumulation_steps: 1
learning_rate: 0.00002
checkpoint:
  save: true
  reload: true
  resume: true
inference_after_reload: true
reason: >-
  The plan defines three student candidates but does not select a winner. Keep this
  configuration non-runnable until the same-harness bake-off selects a student and that
  architecture passes the remote load, optimizer, checkpoint, resume, and inference smoke.
  This configuration does not authorize a model download or training run.
